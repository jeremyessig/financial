{% extends 'layout/main.html.twig' %}

{% block title %}Transaction{% endblock %}

{% block main %}
    <div class="card">
        <div class="card-body d-flex gap-3">

        <button type="button" class="btn btn-outline-primary btn-sm d-flex align-items-center justify-content-center gap-2">
            <twig:ux:icon name="bi:file-earmark-plus" />
            <span>Editer</span>
        </button>        
        <button type="button" class="btn btn-outline-danger btn-sm d-flex align-items-center justify-content-center gap-2">
            <twig:ux:icon name="bi:trash" />
            <span>Supprimer</span>
        </button>
        </div>
    </div>

    <div class="card w-100 h-100">
        <div class="card-header d-flex py-3 justify-content-between align-items-center">
            <h3 class="h3">Toutes les lignes</h3>
            <div class="d-flex">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Recipient's username" aria-label="Recipient's username" aria-describedby="basic-addon2">
                    
                </div>
            </div>
        </div>
        <div class="card-body overflow-auto h-0">

                <div class="row g-3">
                    <div class="col-md-2">
                        <label for="inputId" class="form-label">ID</label>
                        <input class="form-control" id="inputId" value="{{ transaction.id }}" disabled>
                    </div>
                    <div class="col-md-4">
                        <label for="inputValue" class="form-label">Valeur</label>
                        <div class="input-group">
                            <input class="form-control" id="inputValue" value="{{ transaction.type == 'income' ? '+' : '-'}} {{ transaction.value / 100 }}" disabled>
                            <div class="input-group-text">€</div>
                        </div>
                    </div>
                </div>



        </div>
        <div class="card-footer d-flex justify-content-between align-items-center">

        </div>
    </div>


    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transaction.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ transaction.value }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ transaction.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transaction.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ transaction.date ? transaction.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ transaction.type }}</td>
            </tr>
            <tr>
                <th>Bank</th>
                <td>{{ transaction.bank }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transaction_index') }}">back to list</a>

    <a href="{{ path('app_transaction_edit', {'id': transaction.id}) }}">edit</a>

    {{ include('transaction/_delete_form.html.twig') }} #}
{% endblock %}
