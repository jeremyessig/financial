{% extends 'base.html.twig' %}

{% block title %}Establishment{% endblock %}

{% block body %}
    <h1>Establishment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ establishment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ establishment.name }}</td>
            </tr>
            <tr>
                <th>ColorCss</th>
                <td>{{ establishment.colorCss }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_establishment_index') }}">back to list</a>

    <a href="{{ path('app_establishment_edit', {'id': establishment.id}) }}">edit</a>

    {{ include('establishment/_delete_form.html.twig') }}
{% endblock %}
